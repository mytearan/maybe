import { createStore } from 'vuex'

import Auth from '../Warehouse/Auth';
//import AuthCars from '../Warehouse/AuthCars'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    Auth,
   // AuthCars
  }
})
